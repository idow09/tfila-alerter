package com.weiss.ido.tfilaalerter;

import android.os.Bundle;

import org.junit.Test;

public class TfilaAlertFragmentTest {
    @Test
    public void whenDataRetrieved_dataIsShown() throws Exception {
//        TfilaAlertFragment fragment = new TfilaAlertFragment();
        // fragment.setAlertViewModel(mockAlertViewModel)

        // Act
//        fragment.onActivityCreated(Bundle.EMPTY);

        // Assert
        // assertThat(fragment.getAlertText(), is("test alert string"));
    }
}